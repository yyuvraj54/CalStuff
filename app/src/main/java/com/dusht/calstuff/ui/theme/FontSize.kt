package com.dusht.calstuff.ui.theme

import androidx.compose.ui.unit.sp

/**
 * App-wide font size scale for consistency.
 * Use these values instead of hardcoded sp values.
 */
object FontSize {
    /** 9.sp — chart axis labels, smallest annotations */
    val xxxSmall = 9.sp

    /** 10.sp — day labels, tiny captions */
    val xxSmall = 10.sp

    /** 11.sp — secondary labels, small descriptions */
    val xSmall = 11.sp

    /** 12.sp — indicator values, small body text */
    val small = 12.sp

    /** 13.sp — card subtitles, descriptions */
    val smallMedium = 13.sp

    /** 14.sp — card titles, widget headers */
    val medium = 14.sp

    /** 15.sp — emphasized values (e.g. goal kcal) */
    val mediumLarge = 15.sp

    /** 16.sp — body text, button labels */
    val body = 16.sp

    /** 18.sp — section headers, prominent values */
    val large = 18.sp

    /** 22.sp — compact card percentage */
    val xLarge = 22.sp

    /** 24.sp — screen titles */
    val xxLarge = 24.sp

    /** 26.sp — streak subtitle */
    val xxxLarge = 26.sp

    /** 28.sp — ring center kcal number */
    val heading = 28.sp

    /** 32.sp — login title */
    val display3 = 32.sp

    /** 64.sp — large percentage display */
    val display2 = 64.sp

    /** 82.sp — streak number */
    val display1 = 82.sp

    /** 100.sp — fallback emoji / largest display */
    val displayHero = 100.sp
}
